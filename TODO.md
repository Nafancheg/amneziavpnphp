# TODO-бэклог (P0/P1)

Обновлено: 2026-03-30

Легенда статусов:

- `[]` — не начат
- `[~]` — в работе / частично
- `[x]` — завершен

---

## P0

### P0-01. Исправить структуру API-роутов удаления/импорта сервера

[] Убрать ошибочную вложенность и корректно зарегистрировать роуты delete/import/import-history.

- **Проблема:** `Router::delete('/api/servers/{id}/delete', ...)` не имеет закрывающей скобки — callback обрывается, `Router::post('/api/servers/{id}/import', ...)` вложен внутрь delete-handler. Импорт и history роуты фактически не работают.
- Критерии приемки:
  - Эндпоинт удаления сервера отвечает корректно.
  - Эндпоинты импорта и истории импорта зарегистрированы независимо.
  - В существующих API-роутах отсутствуют регрессии.

### ~~P0-02. Исправить доступ к БД в эндпоинте API-токенов~~

[x] Эндпоинт использует `DB::get()->prepare()` — стандартный паттерн. Работает корректно.

### ~~P0-03. Удалить хардкод админ-учетных данных из UI-потока переводов~~

[x] Перевод использует `JWT::requireAuth()`. Хардкода учетных данных нет.

### P0-04. Добавить CSRF-защиту для всех веб-форм, меняющих состояние

[~] CSRF внедрен для 4 из ~36 POST-роутов (switch-container, reattach, detach, delete сервера).

- **Незакрытое:** login, register, deploy, create client, revoke/restore client, language/change, все /api/ POST-роуты — без CSRF.
- Критерии приемки:
  - Каждая POST-форма отправляет CSRF-токен.
  - Запросы без токена/с невалидным токеном отклоняются с 403.
  - Все валидные пользовательские сценарии форм продолжают работать.

### ~~P0-05. Исправить fallback авторизации мониторинга~~

[x] Реализован Bearer + session fallback в `/api/clients/{id}/metrics`.

### ~~P0-06. Исправить логику восстановления бэкапа~~

[x] `restoreBackup()` вызывает public-метод `VpnClient::addClientToServer()`. Статусы согласованы. Нет проблем c видимостью.

### P0-07. Закрыть open redirect в роуте смены языка

[] `$_POST['redirect']` передаётся в `redirect()` без валидации — подтверждённая уязвимость open redirect.

- **Файл:** `public/index.php:2420` — `redirect($_POST['redirect'] ?? '/dashboard')`
- Критерии приемки:
  - Внешние redirect-цели блокируются.
  - Небезопасные значения redirect приводят к безопасному fallback-роуту.

### P0-08. Нормализовать RBAC-модель и совместимость с LDAP

[~] Несогласованность ролей:
- Миграция 001: `ENUM('admin', 'user')`
- Миграция 012: добавляет `'manager', 'viewer'`
- Auth.php (регистрация): назначает роль `'user'`
- LdapSync.php: назначает fallback `'viewer'`

- Критерии приемки:
  - Единая согласованная модель ролей в БД и коде.
  - Создание/обновление LDAP-пользователей использует актуальную схему users.
  - Проверки авторизации работают согласно ролевой политике.

### P0-09. Усилить хранение секретов

[~] SSH-модель доверия закрыта: `StrictHostKeyChecking=accept-new` + managed known_hosts + `escapeshellarg` — всё на месте.

- **Незакрытое:** SSH-пароли (`vpn_servers.password`) хранятся plaintext в БД. LDAP bind password — в `.env` plaintext.
- Критерии приемки:
  - Чувствительные секреты не хранятся в открытом виде.
  - Политика доступа к секретам документирована и соблюдается.

### P0-10. Привести attach flow в соответствие с логикой amnezia-client

[~] Большая часть реализована:

- [x] **10a.** AWG2: чтение junkPacketCount, cookieReplyPacketJunkSize, transportPacketJunkSize, protocolVersion, i1–i5.
- [] **10b.** Создание нового клиента: генерация ключей, поиск IP, добавление [Peer], `syncconf` — нет в attach, частично в VpnClient.
- [x] **10c.** Xray attach: чтение server.json, UUID из inbounds, clientsTable.
- [] **10d.** OpenVPN attach: только определение протокола, нет парсинга server.conf / сертификатов.
- [x] **10e.** Внешний порт контейнера из `docker ps --format`.
- [] **10f.** Составные контейнеры (Cloak, ShadowSocks) — не реализовано.
- [x] **10g.** `docker ps` (не `docker ps -a`).

- Критерии приёмки:
  - Attach корректно распознаёт все типы контейнеров Amnezia.
  - Панель может создать нового VPN-клиента для любого поддерживаемого протокола.
  - Составные контейнеры (Cloak, SS) обрабатываются как мульти-протоколы.

---

## P1

### P1-01. Единый операционный дашборд с алертами

[] Одностраничный обзор состояния серверов и рисков по клиентам.

### P1-02. Поиск, фильтры и массовые операции по клиентам

[] Фильтрация по статусу/сроку/лимитам, поиск по имени/IP, bulk revoke/restore.

### P1-03. Фоновая очередь задач

[] Deploy/import/backup/restore как async jobs с прогрессом.

### P1-04. Безопасный bootstrap первого входа

[] Обязательная смена пароля, блокировка дефолтов в production.

### P1-05. Аудит-лог критичных действий

[] Логирование auth, изменений серверов/клиентов/лимитов; UI-фильтры.

### P1-06. Стандартизация API-контракта

[] Единый формат ответов, версионирование, документация.

### P1-07. Уведомления по ключевым операционным событиям

[] Добавить настраиваемые уведомления по критичным событиям.

- Критерии приемки:
  - Поддерживаются события expired, over-limit, deploy failed, backup failed.
  - Поддержан как минимум один канал доставки.
  - Реализована дедупликация и обработка повторных отправок.

### P1-08. Добавить базовый quality gate с автоматическими проверками

[] Добавить автотесты и обязательные проверки качества перед merge.

- Критерии приемки:
  - Критичные auth/API-сценарии покрыты тестами.
  - Миграции валидируются на чистой базе данных.
  - CI (или эквивалент) запускает проверки перед слиянием.
